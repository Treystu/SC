/**
 * Unified configuration system exports
 * Single source of truth for all configuration functionality
 */

export { ConfigManager, configManager } from './ConfigManager';
export type { AppConfig, Environment, ConfigUpdateOptions, ConfigValidationResult } from '../types/config';
