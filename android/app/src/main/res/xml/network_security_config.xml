<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration: Prevent cleartext traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        No certificate pinning required: Sovereign Communications is fully decentralized (no central API).
        This config trusts only system CAs for peer-to-peer connections.
    -->

    <!--
        Development/Debug Configuration

        This allows user-installed CA certificates in debug builds for testing.
        In production, this section is automatically disabled.
    -->
    <debug-overrides>
        <trust-anchors>
            <!-- Additionally trust user-added CAs (e.g., Charles Proxy, mitmproxy) -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
